import React from 'react'

import Logout from '../Logout/Logout'

const Header: React.FC = () => {
  return (
    <header>
      <p>logo</p>
      <Logout />
    </header>
  )
}

export default Header
